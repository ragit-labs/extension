import { NoteBlock } from "../types";


const NoteBlockComp: React.FC<NoteBlock> = ({
    content,
    block_type,
    extra_metadata
}) => {
    return (
        <div>
            <h1>Test</h1>
        </div>
    )
};